---
const currentPath = Astro.url.pathname;
---

<header class="bg-gradient-to-r from-blue-600 to-purple-600 text-white shadow-lg">
  <nav class="container mx-auto px-6 py-4">
    <div class="flex items-center justify-between">
      <a href="/" class="text-2xl font-bold hover:text-blue-200 transition">
        📊 DataViz Studio
      </a>
      <ul class="flex gap-6">
        <li>
          <a 
            href="/" 
            class={`hover:text-blue-200 transition font-medium ${currentPath === '/' ? 'text-blue-200 border-b-2 border-blue-200' : ''}`}
          >
            Accueil
          </a>
        </li>
        <li>
          <a 
            href="/penguins" 
            class={`hover:text-blue-200 transition font-medium ${currentPath === '/penguins' ? 'text-blue-200 border-b-2 border-blue-200' : ''}`}
          >
            Penguins
          </a>
        </li>
        <li>
          <a 
            href="/population" 
            class={`hover:text-blue-200 transition font-medium ${currentPath === '/population' ? 'text-blue-200 border-b-2 border-blue-200' : ''}`}
          >
            Population
          </a>
        </li>
        <li>
          <a 
            href="/etablissements" 
            class={`hover:text-blue-200 transition font-medium ${currentPath === '/etablissements' ? 'text-blue-200 border-b-2 border-blue-200' : ''}`}
          >
            Établissements
          </a>
        </li>
      </ul>
    </div>
  </nav>
</header>
